import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  feedback: {
    margin: 0,
    padding: 0,
    color: "#f44336",
    fontFamily: "Roboto",
    textAlign: "left",
    marginLeft: "14px",
    marginTop: "3px",
    fontSize: '0.75rem'
  },
}));
